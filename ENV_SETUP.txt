# Environment Variables Needed for Deployment
# Copy these to your hosting platform's environment variables section

# Stripe Configuration
# Get your keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Email — RECOMMENDED on Railway / Render (outbound SMTP is often blocked on hobby plans):
# Sign up at https://resend.com — verify your domain (e.g. lonclinic.com), create an API key:
# RESEND_API_KEY=re_xxxxxxxx
# EMAIL_FROM must use your verified domain, e.g. Lon Clinic <info@lonclinic.com>
# CONTACT_EMAIL=info@lonclinic.com

# Email Configuration (SMTP) — works on VPS or Railway Pro where ports 465/587 are allowed
# Option 1: Use info@lonclinic.com (if created with Namecheap/Google Workspace)
# EMAIL_HOST=mail.privateemail.com  (for Namecheap)
# EMAIL_HOST=smtp.gmail.com  (for Google Workspace)
# EMAIL_PORT=587
# EMAIL_USER=info@lonclinic.com
# EMAIL_PASS=your_email_password_here
# EMAIL_FROM=Longevity Clinic <info@lonclinic.com>

# Option 2: Use Gmail personal account
# For Gmail, create App Password: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password_here
EMAIL_FROM=Longevity Clinic <your_email@gmail.com>

# Doxy.me Video Consultation
DOXY_ROOM_URL=https://doxy.me/lonclinic/ritaaguiar

# Google Calendar (hide busy times on bookable slots)
# 1. Google Cloud Console → APIs & Services → Enable "Google Calendar API"
# 2. OAuth consent screen: External + Testing, add Rita's Gmail as a test user
# 3. Create OAuth client ID (Web application)
# 4. Authorized redirect URI (must match exactly):
#    https://www.lonclinic.com/api/admin/google-calendar/callback
#    Local: http://localhost:3000/api/admin/google-calendar/callback
# 5. Then in Admin → Availability → choose Dra. Rita Aguiar → Connect Google Calendar
GOOGLE_CALENDAR_CLIENT_ID=
GOOGLE_CALENDAR_CLIENT_SECRET=
# Optional override if the callback is not on https://www.lonclinic.com
# GOOGLE_CALENDAR_REDIRECT_URI=https://www.lonclinic.com/api/admin/google-calendar/callback

# Contact Email (shown on website footer)
CONTACT_EMAIL=info@lonclinic.com

# Server Port (usually set automatically by hosting platform)
PORT=3000
